# API server
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.9
stripe>=11.0.0

# Core dependencies
anthropic>=0.79.0
playwright>=1.40.0
playwright-stealth>=2.0.0
beautifulsoup4>=4.14.3
pdfplumber>=0.11.0
browser-use>=0.11.9
2captcha-python>=1.2.0
firecrawl-py>=4.15.1
pandas>=3.0.0
pydantic>=2.12.5
pydantic-settings>=2.13.0
python-dotenv>=1.2.1
pyyaml>=6.0.3
requests>=2.32.5
rapidfuzz>=3.10.0
usaddress>=0.5.10

# JWT (admin upload layer) + password hashing (white-label auth)
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
email-validator>=2.0.0

# File format readers
dbfread>=2.0.7
xlrd>=2.0.1

# Phone normalization
phonenumbers>=8.13

# Slack SDK — county-launch interactive buttons (admin_router._update_slack_message)
slack-sdk>=3.33.0

# SMS / voice — Telnyx (Twilio replaced 2026-05-11; see mellow-strolling-fairy plan)
telnyx>=2.1.0
PyNaCl>=1.5.0    # Ed25519 verification for Telnyx webhook signatures

# Database dependencies
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
alembic>=1.13.0

# Cache / rate-limiting (server-side only — not available locally)
redis>=5.0.0

# Agents / LangGraph (Cora runtime — Phase 2B)
langgraph>=1.1.0
langgraph-checkpoint-postgres>=3.0.0
psycopg[binary]>=3.2.0
langsmith>=0.7.0

# Sandbox — in-memory Redis shim for scenario tests
fakeredis>=2.24

# HTML templating (daily_dashboard PDF generator)
jinja2>=3.1.0

# Image generation (Stage 5: deal-win graphic, social proof wall)
Pillow>=10.0.0

# CDS scoring fit (Stage C of cross-county retune — src/services/scoring_fit.py)
# numpy is already pulled in transitively by pandas.
scikit-learn>=1.5.0

# Prometheus metrics exposition (Stage 10 — /metrics endpoint for Alertmanager pipeline)
prometheus_client>=0.21.0

openpyxl>=3.1.0
