# Per-scraper config for roofing_permit.
# Loader: src.utils.scraper_config.get_scraper_config('roofing_permit') / get_selectors('roofing_permit')
# Reserved for future config — populated as needed during migration.
