# Per-scraper config for bankruptcy.
# Loader: src.utils.scraper_config.get_scraper_config('bankruptcy') / get_selectors('bankruptcy')
# Reserved for future config — populated as needed during migration.
