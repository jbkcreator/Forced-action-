# Per-scraper config for insurance.
# Loader: src.utils.scraper_config.get_scraper_config('insurance') / get_selectors('insurance')
# Reserved for future config — populated as needed during migration.
