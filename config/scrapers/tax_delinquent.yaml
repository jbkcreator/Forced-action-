# Per-scraper config for tax_delinquent.
# Loader: src.utils.scraper_config.get_scraper_config('tax_delinquent') / get_selectors('tax_delinquent')
# Reserved for future config — populated as needed during migration.
