# Per-scraper config for evictions.
# Loader: src.utils.scraper_config.get_scraper_config('evictions') / get_selectors('evictions')
# Reserved for future config — populated as needed during migration.
